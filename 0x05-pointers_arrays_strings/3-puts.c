#include "main.h"

/**
 *_puts - print a string followed by new line
 *@str: input string
 *Return: no return
 */

void _puts(char *str)
{
	int i = 0;

	while (i >=0 )
	{
		if (str[i]  == '\0')
		_putchar('\n');
	         break;
	}

	_putchar('\n')
	i++;

}
